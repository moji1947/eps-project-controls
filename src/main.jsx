import './index.css'

// The original project-control mockup is a self-contained React build. Keep
// it intact so every original screen and interaction remains available.
window.location.replace(`${import.meta.env.BASE_URL}mockup/index_1.html`)
